import Extraction from "./extraction";

export default function Page() {
  return <Extraction />;
}

export const metadata = {
  title: "Continuum - Event Extraction",
  description:
    "Map new event attributes from existing event attributes with Regex",
};
